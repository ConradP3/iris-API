# Simple Iris Classification Model Deployed Using Docker & Flask

## Requirements
This Lab requires Docker and Python 3+

## Setup
```
git clone 
cd iris-API
sudo docker build -t iris-api .
docker run -p 5000:5000 iris-api
```

